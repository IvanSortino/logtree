% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leaves.R
\name{log_warn}
\alias{log_warn}
\title{Log a warning leaf line}
\usage{
log_warn(msg, close = FALSE)
}
\arguments{
\item{msg}{Character scalar.}

\item{close}{Logical. When \code{TRUE}, force-close the enclosing section after
this line: the line is shown as the section's terminal (corner) line and
its \code{Done} line is suppressed. Defaults to \code{FALSE}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Also elevates the currently-open step's status to \code{"warning"} (unless it
is already \code{"error"}), so the step's close line renders the elevated
glyph even though the enclosing function returns normally.
}
\examples{
logtree_reset()
log_warn("Retry 1/3 due to timeout")
}
